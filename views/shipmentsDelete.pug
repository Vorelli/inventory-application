extends layout

block content
  .content
    .title
      h1= title
    div(style='color: red;width: 100%; text-align: center')= 'Are you sure you want to delete ' + shipment._id + '?'
    form(method='POST' action='').delete
      .formGroup
        input(type='hidden' name='shipmentid' required='true' value=shipment._id)
      .formGroup
        button(type='submit') Delete
