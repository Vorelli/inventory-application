extends layout

block content
  .content
    .title
      h1= title
    ul
      each item in inventory
        li.productWrapper
          a(href=item.url)
            .item
              img(src='/images/' + item.name + '.jpg')
              p= item.name